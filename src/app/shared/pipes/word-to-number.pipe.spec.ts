import { WordToNumberPipe } from './word-to-number.pipe';

describe('WordToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WordToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
